[project]
name = "mixtec-parser"
version = "0.1.0"
description = "Parser, interpreter, and Streamlit demo for a formal grammar of Mixtec codex scenes"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "loguru>=0.7.3",
    "streamlit>=1.58.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
